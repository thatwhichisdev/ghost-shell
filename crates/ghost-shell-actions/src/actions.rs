gpui::actions!(
    ghost_shell,
    [
        /// Action to open finder window
        FinderOpen,
        /// Action to close finder window
        FinderClose,
        /// Action to toggle finder window between two possible state open/close
        FinderToggle,
        /// Action to open launcher window
        LauncherOpen,
        /// Action to close launcher window
        LauncherClose,
        /// Action to toggle launcher window between two possible state open/close
        LauncherToggle,
    ]
);
